% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RDD_extrapolate_bootstrap.R
\name{RDD_extrapolate_bootstrap}
\alias{RDD_extrapolate_bootstrap}
\title{Multiplier Bootstrap for RDD Extrapolation Estimates}
\usage{
RDD_extrapolate_bootstrap(
  result,
  Y,
  X,
  D,
  kernel = "gaussian",
  weights = rep(1, length(Y)),
  num_folds = 5,
  order = 1,
  batch_size = 1000L,
  points = 100L,
  bootstrap_iter = 100L,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{result}{A fitted object returned by
\code{\link[=RDD_extrapolate_CV_band]{RDD_extrapolate_CV_band()}}. It must contain \code{plotting0}, \code{plotting1},
\code{band0}, and \code{band1}.}

\item{Y}{A numeric vector of observed outcomes.}

\item{X}{A numeric matrix or data frame of assignment variables or covariates.
Rows correspond to observations and columns correspond to covariates.}

\item{D}{A numeric or integer vector indicating treatment status. Values
should be \code{1} for treated observations and \code{0} for untreated observations.}

\item{kernel}{A character string specifying the kernel used in the local
polynomial regressions. Available choices are \code{"gaussian"}, \code{"uniform"},
and \code{"triangular"}.}

\item{weights}{An optional numeric vector of observation weights. The default
assigns weight one to every observation.}

\item{num_folds}{A positive integer giving the number of folds used for
cross-validation. The default is \code{5}.}

\item{order}{A nonnegative integer giving the total order of the local
polynomial. The default \code{1} corresponds to local linear regression.}

\item{batch_size}{A positive integer giving the maximum number of prediction
points processed in one batch. Smaller values reduce peak memory usage but
may increase computation time. The default is \code{1000}.}

\item{points}{A positive integer giving the number of points in the
evaluation grids for \code{q0} and \code{q1}. The default is \code{100}.}

\item{bootstrap_iter}{A positive integer giving the number of multiplier
bootstrap draws. The default is \code{100}.}

\item{parallel}{A logical value indicating whether the bootstrap draws should
be evaluated using a parallel socket cluster. The default is \code{FALSE}.}

\item{n_cores}{An optional positive integer giving the number of parallel
workers. When \code{parallel = TRUE} and \code{n_cores = NULL}, the function uses one
fewer than the number of detected physical cores, subject to a minimum of
one and a maximum equal to \code{bootstrap_iter}.}
}
\value{
A named list containing:
\itemize{
\item \code{q0_grid} and \code{q1_grid}: evaluation grids.
\item \code{q0_draws} and \code{q1_draws}: matrices of bootstrap curve evaluations, with
one bootstrap draw per column.
\item \code{y0_draws} and \code{y1_draws}: matrices of bootstrap conditional mean
potential-outcome estimates, with one draw per column.
\item \code{multipliers}: matrix of standard exponential bootstrap multipliers.
\item \code{complete_cases}: complete-case indicator for the original inputs.
\item \code{band0} and \code{band1}: bandwidths inherited from the original fit.
\item \code{bootstrap_iter}: number of bootstrap draws.
\item \code{points}: number of curve-evaluation points.
}
}
\description{
Computes multiplier-bootstrap draws for the estimated potential-outcome
mappings and imputed conditional mean potential outcomes produced by
\code{\link[=RDD_extrapolate_CV_band]{RDD_extrapolate_CV_band()}}.
}
\details{
Independent standard exponential multipliers are applied to the observation
weights. The model is then re-estimated for each bootstrap draw using the
bandwidth pair selected in the original fit.

For bootstrap draw \eqn{b}, the effective observation weight is

\deqn{w_i e_{ib},}

where \eqn{w_i} is the original observation weight and \eqn{e_{ib}} is an
independent standard exponential multiplier.

The function stores draws of the estimated \code{q0} and \code{q1} curves and draws of
the imputed conditional mean potential outcomes. It does not itself construct
confidence intervals; the stored draws are consumed by
\code{\link[=plot_q_results]{plot_q_results()}} and \code{\link[=counterfactual_policy_effect]{counterfactual_policy_effect()}}.

Set the random seed before calling this function when reproducible bootstrap
multipliers are required.
}
\seealso{
\code{\link[=RDD_extrapolate_CV_band]{RDD_extrapolate_CV_band()}}, \code{\link[=plot_q_results]{plot_q_results()}},
\code{\link[=counterfactual_policy_effect]{counterfactual_policy_effect()}}
}
