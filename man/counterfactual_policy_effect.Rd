% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counterfactual_policy_effect.R
\name{counterfactual_policy_effect}
\alias{counterfactual_policy_effect}
\title{Estimate the Effect of a Counterfactual Treatment Policy}
\usage{
counterfactual_policy_effect(
  result,
  bootstrap_result = NULL,
  policy,
  Y,
  X,
  D,
  level = 0.9
)
}
\arguments{
\item{result}{A fitted object returned by
\code{\link[=RDD_extrapolate_CV_band]{RDD_extrapolate_CV_band()}}. It must contain \code{y0}, \code{y1}, and \code{S}.}

\item{bootstrap_result}{An optional bootstrap object returned by
\code{\link[=RDD_extrapolate_bootstrap]{RDD_extrapolate_bootstrap()}}. When supplied, the function calculates
multiplier-bootstrap confidence intervals. The default is \code{NULL}.}

\item{policy}{A function that accepts the covariate matrix \code{X} and returns
one value per observation. Returned values must lie between zero and one
and represent treatment probabilities under the counterfactual policy.}

\item{Y}{A numeric vector of observed outcomes.}

\item{X}{A numeric matrix or data frame of assignment variables or covariates.
Rows correspond to observations and columns correspond to covariates.}

\item{D}{A numeric or integer vector indicating treatment status. Values
should be \code{1} for treated observations and \code{0} for untreated observations.}

\item{level}{A numeric scalar between zero and one specifying the confidence
level. The default is \code{0.90}.}
}
\value{
A named list containing:
\itemize{
\item \code{estimate}: estimated counterfactual policy effect.
\item \code{conf_low} and \code{conf_high}: confidence interval endpoints.
\item \code{conf_rad}: confidence radius.
\item \code{level}: requested confidence level.
\item \code{bootstrap_estimates}: vector of bootstrap policy-effect estimates, or
\code{NULL} when no bootstrap object is supplied.
\item \code{num_affected}: expected number of observations with \code{S = 1} whose
treatment status changes.
\item \code{affected_share}: \code{num_affected} divided by the number with \code{S = 1}.
\item \code{policy_prob}: policy treatment probabilities.
\item \code{switch_prob}: probability that treatment status differs from observed
treatment status.
\item \code{indv_contribution}: observation-level contributions to the numerator.
\item \code{S}: identification indicator used in estimation.
\item \code{identified_n}: number of observations with \code{S = 1}.
\item \code{complete_cases}: complete-case indicator for the original inputs.
}
}
\description{
Estimates the mean causal effect of replacing the observed deterministic
treatment assignment with a user-specified counterfactual treatment policy.
The effect is evaluated over observations whose conditional average treatment
effects are identified, as indicated by \code{result$S == 1}.
}
\details{
The counterfactual policy may be deterministic or probabilistic. A
deterministic policy returns only zero and one, while a probabilistic policy
returns treatment probabilities between zero and one.

Let \code{p(X)} denote the treatment probability under the counterfactual policy.
For observations currently untreated, the function uses the estimated treated
potential outcome when the counterfactual policy assigns treatment. For
observations currently treated, it uses the estimated untreated potential
outcome when the counterfactual policy removes treatment.

The reported estimand is

\deqn{
\frac{1}{\sum_i S_i}
\sum_i S_i \left[
(1-D_i)p(X_i)(\widehat{Y}_i(1)-Y_i)
+
D_i(1-p(X_i))(\widehat{Y}_i(0)-Y_i)
\right].
}

Consequently, the effect is averaged over all observations with \code{S = 1},
rather than only over observations whose treatment status changes.

When \code{bootstrap_result} is supplied, the confidence radius is the requested
quantile of the absolute bootstrap deviations from the original estimate.
}
