% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RDD_extrapolate_CV_band.R
\name{RDD_extrapolate_CV_band}
\alias{RDD_extrapolate_CV_band}
\title{Extrapolate Potential Outcomes in a Sharp RDD Using Comonotonicity}
\usage{
RDD_extrapolate_CV_band(
  Y,
  X,
  D,
  kernel = "gaussian",
  bands = seq(0.01, 1, length.out = 100),
  weights = rep(1, length(Y)),
  num_folds = 5,
  order = 1,
  batch_size = 1000L
)
}
\arguments{
\item{Y}{A numeric vector of observed outcomes. Its length must equal the number of rows of \code{X}.}

\item{X}{A numeric matrix or data frame of assignment variables or covariates.
Rows correspond to observations and columns correspond to covariates.}

\item{D}{A numeric or integer vector indicating treatment status. Values
should be \code{1} for treated observations and \code{0} for untreated observations.}

\item{kernel}{A character string specifying the kernel used in the local
polynomial regressions. Available choices are \code{"gaussian"}, \code{"uniform"},
and \code{"triangular"}.}

\item{bands}{A numeric vector specifying bandwidths.

When \code{length(bands) == 2}, the first and second values are used as the
first-stage bandwidths for the untreated and treated regressions,
respectively.

When \code{length(bands) > 2}, the values are treated as candidate bandwidths
and bandwidth selection is performed by cross-validation.

A length-one vector is currently not accepted.}

\item{weights}{An optional numeric vector of observation weights. The default
assigns weight one to every observation.}

\item{num_folds}{A positive integer giving the number of folds used for
cross-validation. The default is \code{5}.}

\item{order}{A nonnegative integer giving the total order of the local
polynomial. The default \code{1} corresponds to local linear regression.}

\item{batch_size}{A positive integer giving the maximum number of prediction
points processed in one batch. Smaller values reduce peak memory usage but
may increase computation time.}
}
\value{
A named list containing:
\itemize{
\item \code{g1_imputed_func}: a function that imputes the conditional mean treated
potential outcome from the estimated untreated conditional mean.
\item \code{g0_imputed_func}: a function that imputes the conditional mean untreated
potential outcome from the estimated treated conditional mean.
\item \code{g1_func}: the estimated conditional mean outcome function fitted using the
treated sample.
\item \code{g0_func}: the estimated conditional mean outcome function fitted using the
untreated sample.
\item \code{q1}: the estimated mapping from untreated to treated conditional mean
potential outcomes.
\item \code{q0}: the estimated mapping from treated to untreated conditional mean
potential outcomes.
\item \code{plotting1} and \code{plotting0}: two-column matrices containing the generated
regressors and observed outcomes used in the second-stage local polynomial
regressions near the treatment frontier.
\item \code{y0}: estimated conditional mean untreated potential outcomes for the
complete-case observations.
\item \code{y1}: estimated conditional mean treated potential outcomes for the
complete-case observations.
\item \code{S}: an integer support indicator. A value of \code{1} indicates that the
observation's factual conditional mean lies within the estimated domain of
the relevant counterfactual mapping.
\item \code{band0} and \code{band1}: the first-stage bandwidths used for the untreated and
treated regressions.
\item \code{y0_min}, \code{y0_max}, \code{y1_min}, and \code{y1_max}: estimated endpoints of the
second-stage support regions.
}
}
\description{
The function first estimates conditional mean outcomes separately for treated
and untreated observations using multivariate local polynomial regression. It
then identifies observations near the treatment frontier using nearest
neighbors and estimates the mappings between treated and untreated conditional
mean potential outcomes.
}
